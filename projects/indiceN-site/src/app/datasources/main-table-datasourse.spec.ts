import { MainTableDatasourse } from './main-table-datasourse';

describe('MainTableDatasourse', () => {
  it('should create an instance', () => {
    expect(new MainTableDatasourse()).toBeTruthy();
  });
});
