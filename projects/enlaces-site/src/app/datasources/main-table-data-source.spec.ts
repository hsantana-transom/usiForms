import { MainTableDataSource } from './main-table-data-source';

describe('MainTableDataSource', () => {
  it('should create an instance', () => {
    expect(new MainTableDataSource()).toBeTruthy();
  });
});
